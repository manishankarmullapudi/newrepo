version: 0.2
 
phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo Build started
   
artifacts:
  type: 
  files:
    - index.html
    - appspec.yml
    - scripts/install_dependencies
    - scripts/start_server
